import 'package:flutter/material.dart';

class SafeHomeBox extends StatelessWidget {
  const SafeHomeBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
